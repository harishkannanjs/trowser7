import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['framerusercontent.com', 'images.unsplash.com', 'images.pexels.com']
  }
};

export default nextConfig;
